#Input distance (in km) and time (in hours). Calculate speed using:
distance = int(input("Enter the distance:"))
time = int(input("Enter the time:"))
speed = distance/time
print(f"speed is {speed} Km/h")